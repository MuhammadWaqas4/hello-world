import React from 'react';

function Brak(props){
    return(
        <div>
            <h1> Break Fast starts Timings are: {props.start}</h1>
            <h2> Break Fast finish Timings are: {props.finish}</h2>
        </div>
    )
}
export default Brak;